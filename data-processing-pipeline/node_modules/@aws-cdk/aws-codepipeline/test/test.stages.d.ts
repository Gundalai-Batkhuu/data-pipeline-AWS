import { Test } from 'nodeunit';
declare const _default: {
    'Pipeline Stages': {
        'can be inserted at index 0'(test: Test): void;
        'can be inserted before another Stage'(test: Test): void;
        'can be inserted after another Stage'(test: Test): void;
        'attempting to insert a Stage at a negative index results in an error'(test: Test): void;
        'attempting to insert a Stage at an index larger than the current number of Stages results in an error'(test: Test): void;
        "attempting to insert a Stage before a Stage that doesn't exist results in an error"(test: Test): void;
        "attempting to insert a Stage after a Stage that doesn't exist results in an error"(test: Test): void;
        "providing more than one placement value results in an error"(test: Test): void;
    };
};
export = _default;
